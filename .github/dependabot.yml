version: 2
updates:
  # Gradle deps are intentionally pinned for v1 (see build.gradle). Security
  # alerts still fire independently of this file; we just don't want noisy
  # version-update PRs.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    labels:
      - dependencies
      - ci
